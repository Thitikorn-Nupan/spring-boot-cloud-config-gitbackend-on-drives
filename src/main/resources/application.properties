# Default it give you your name app
# If you need to config gitbackend server
# Change it to the name server
# spring.application.name=spring-cloud-config-git-repository
# ********** New solution for access another name properties
# Just use name it to be your spring.application.name
# this case i try to access gitbackend-on-driver-development.properties so i change name to gitbackend-on-driver-development
# Or you can get multiple name (multiple properties files)
spring.application.name=gitbackend-on-driver-development,gitbackend-on-driver,gitbackend-on-driver-client
server.port=8080
# Active Profile - will relate to development properties file in the server.
# If this property is absent then,default profile will be activated which is
# the property file without any environment name at the end.
# spring.profiles.active=development Not work!!
# config to my url spring cloud server
spring.cloud.config.uri=http://localhost:8888
# management.security.enabled=false
